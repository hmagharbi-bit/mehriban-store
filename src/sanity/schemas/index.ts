import { parfumSchema } from './parfum';
import { productSchema } from './product';
import { orderSchema } from './order';
import { homeSchema } from './home';

export const schemaTypes = [parfumSchema, productSchema, orderSchema, homeSchema];
